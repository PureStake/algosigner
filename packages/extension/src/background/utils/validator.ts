///
// Allowed field types.
///
export enum FieldType {
    
}

///
// Return field if valid based on type, throw error if invalid.
///
export function validate(field: any, type: FieldType): any {
    return field;
}