/**
 * @license
 * Copyright 2020 
 * =========================================
*/

// import { EncryptionWrap } from "../encryptionWrap"

// export interface IEncryptionWrap extends EncryptionWrap {

//     // Use the passphrase to encrypt the addtional parameter objects
//     // Returns: State of success (true|false)
//     lock(passphrase: string): Promise<boolean>;

//     // Use the passphrase to decrypt the additional parameter objects
//     // Returns: Array of unlocked Uint8Array objects
//     unlock(passphrase: string): Promise<Uint8Array[]>;
    
// }